# Fixture for test-scan-stale-notes.sh - every line below is a FAKE stale incident note.

The ANTHROPIC_API_KEY was exposed in chat today and needs rotation.
This token must be rotated before use.
FAKE_TOKEN: [MISSING - NEEDS ROTATION]
The old credential was leaked during the demo session.
Treat the FAKE_EXAMPLE_KEY as compromised until further notice.

Negative controls (must NOT be flagged):
- Rotate credentials on a yearly schedule as routine maintenance.
- The rotation runbook lives in the rotate-secret skill.
