# claude.ai skills inventory (generated - do not edit)

Do NOT paste static skill copies: a pasted `SKILL.md` goes stale. With the Schnapp Portal
connector on (default), claude.ai reads skills LIVE from `skills/<name>/SKILL.md` on
demand, so the substance stays current with zero registration. This is the generated
inventory of what is available to read live, from
[`scripts/gen-claude-ai-skills.sh`](../scripts/gen-claude-ai-skills.sh), so it never drifts.
Tier is each skill's own `claude-ai-tier:` frontmatter. Optional: register a THIN auto-trigger
stub (a pointer body that reads the live SKILL.md, never a copy) for a skill you want the
platform to surface by description without naming it.

## Core (run proactively; worth a thin auto-trigger stub)

- [ ] **notes-lookup**: Use when the answer likely lives in the owner's OWN notes/knowledge - a past decision, project context, a runbook, a domain fact, or …
- [ ] **session-hygiene**: Use on surfaces WITHOUT hooks (claude.ai web/chat, iPhone, Cowork) to run the must-happen session procedures that Claude Code does …
- [ ] **surface-check**: Use when the user asks "what's loaded here", "what can this surface do", "is my memory/rules/connector active", "why doesn't X work here", …

## On-demand (read live when a task matches)

- [ ] **agentic-os-reference**: Use when a session needs the domain theory behind this system - "how do skills/hooks/agents actually load", "what is a hook and when does …
- [ ] **appfolio**: Use when integrating AppFolio - pulling AppFolio custom reports or the AppFolio Reporting/Data API (units, residents, occupancy, rent roll, …
- [ ] **cleanse-secrets**: Use when files may contain literal secret VALUES that must be found and removed - auditing a repo/diff before commit or sharing, …
- [ ] **context-budget**: Use when a session feels sluggish or output quality is degrading, after adding many skills/agents/MCP servers, when you want to know how …
- [ ] **council**: Use when a decision has multiple credible paths and no obvious winner, when the owner asks for second opinions, dissent, or multiple …
- [ ] **etl-pipeline-build**: Use when building or changing a Python ETL pipeline that loads SQL Server - extract from an API / file / vendor source, transform, and …
- [ ] **grill-me**: Use when the owner wants a plan or design stress-tested before building it, asks to "grill me", to "poke holes", to be interviewed about a …
- [ ] **intent-check**: Use before acting on an ambiguous, high-stakes, or "am I even asking the right thing" request - reason from the literal words to the TRUE …
- [ ] **learn-route**: Use immediately after a correction or capture lands - to classify what kind of knowledge it is and route it to the right destination via …
- [ ] **os-architecture-contract**: Use when you need to know WHY schnapp-os is shaped the way it is before changing it - "why is this a symlink not a plugin", "can I add a …
- [ ] **os-build-and-env**: Use when recreating or repairing the schnapp-os environment on any surface - "set up a new Mac", "fresh machine install", "wire up …
- [ ] **os-change-control**: Use when landing ANY change to schnapp-os and you need to know how changes are classified and gated - "should I branch or PR", "do I need …
- [ ] **os-config-and-flags**: Use when you need to find, inspect, or change ANY configuration in schnapp-os - "where is X configured", "what hooks are wired", "which env …
- [ ] **os-cross-surface-campaign**: Use to run or resume the cross-surface enablement campaign - "make my rules/skills/memory work on every surface", "is the second brain …
- [ ] **os-debugging-playbook**: Use when something in schnapp-os is broken and you need the known-failure-mode triage before inventing a theory - "401 Invalid bearer …
- [ ] **os-diagnostics-and-tooling**: Use when you need to MEASURE the state of schnapp-os instead of eyeballing it - "run all the checks", "is the repo clean enough to push", …
- [ ] **os-docs-and-writing**: Use when writing or updating ANY schnapp-os document of record - "where does this fact/status/decision go", "how do I write an ADR / …
- [ ] **os-failure-archaeology**: Use before re-investigating anything that smells already-fought in schnapp-os - "why is this a rule", "was this tried before", "should we …
- [ ] **os-proof-and-analysis-toolkit**: Use when a claim about schnapp-os needs PROOF instead of trust - "is this token actually malformed", "does that surface really read the …
- [ ] **os-research-frontier**: Use when picking the NEXT ambitious improvement to schnapp-os itself - "what should this system do that no agentic OS does yet", "where can …
- [ ] **os-research-methodology**: Use when a hunch, theory, or improvement idea for schnapp-os needs to become an accepted change or a documented rejection - "I think the …
- [ ] **os-run-and-operate**: Use to OPERATE the running schnapp-os system - "restart mac-mcp / obsidian-mcp", "is the connector up", "where are the logs", "redeploy …
- [ ] **os-validation-and-qa**: Use when deciding whether schnapp-os work is actually DONE - "can I mark this [x]", "what counts as verified", "how do I run the self-test …
- [ ] **performance**: Use when something is slow or must be proven fast - measuring a performance baseline, detecting a regression before/after a change, …
- [ ] **pr-sweep**: Use to clear open pull requests across the SchnappAPI org on demand - "are there open PRs", "close the stray PRs", "never leave open PRs", …
- [ ] **quickbase**: Use when integrating Quickbase - calling the Quickbase JSON RESTful API (query/insert/update records), handling app IDs / table IDs (DBIDs) …
- [ ] **rotate-secret**: Use when a secret value must be replaced everywhere it lives - it leaked or is suspected compromised, it is being renamed/split during the …
- [ ] **rules-distill**: Use when reusable lessons have piled up uncaptured: after a stocktake, during periodic maintenance, or when several skills or sessions keep …
- [ ] **session-to-skill**: Use after a session where a non-obvious, repeatable multi-step procedure was worked out (a way of pulling two sources in a specific order, …
- [ ] **settlement-audit**: Use to independently audit schnapp-bet's NBA prop grading/settlement - re-derive Won/Lost from the final boxscore stat vs the stored …
- [ ] **sql-server-patterns**: Use when writing or reviewing T-SQL for SQL Server 2022 - upserts/MERGE, idempotent schema changes (CREATE OR ALTER, IF NOT EXISTS), …
- [ ] **status**: Use when the user asks "what's the state of everything", "status", "is anything stale/unmerged/unpushed", "are the connectors/services up", …
- [ ] **vault-resolve**: Use when you need a secret's actual value at runtime - reading an API key/token/password, wiring a new env var, or a script needs a …

