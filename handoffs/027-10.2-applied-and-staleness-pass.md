# Handoff 027 — 10.2 applied; staleness pass; op-mcp outage + brain test-pollution findings

Date: 2026-06-16. Surface: Claude Code (web remote container) + reports relayed from a Cowork-on-Mac
session. Status: COMPLETE on `main` (pending commit of this handoff). Wrap of the 10.2 + staleness
work; continues handoffs 025/026.

## What happened
1. **10.2 applied by owner** across claude.ai web / iPhone / Cowork:
   - Skills uploaded as zips on claude.ai (session-hygiene, surface-check, docs-lookup — packaged
     this session); connectors enabled; `claude-kit-core` plugin added in Cowork.
   - Always-loaded block placed **globally** at **Settings > Profile > Preferences** (owner choice,
     not a per-Project field) → applies to every chat and to iPhone on the same account. Cowork has
     its own copy.
   - PLAN **10.2 → [x]**. Per-surface `surface-check` confirmation + the "does Cowork run hooks"
     answer roll into **10.3**.
2. **Verified finding — hosted op-mcp is DOWN (host-side):** `op_health` errors `authentication
   error … Check OP_SERVICE_ACCOUNT_TOKEN on the host` from **two** independent surfaces (this Code
   session + Cowork). Not a per-surface connector issue — the Render host's SA token. Mac
   `op_run`/`op_inject` is the working route meanwhile. **Final-verification #7 currently FAILS.**
3. **Staleness pass** (the owner's "make sure nothing is stale"): see below.
4. **Verified finding — brain is 100% test pollution:** `get_index` shows 6 notes, all
   pipeline-verification TEST notes from 2026-06-16 (cluster `infrastructure`), 2 flagged actionable
   test-closeouts, zero real notes. Cleanup pending owner confirm.

## Staleness fixes committed (anti-stale method: one canonical status, others point to it)
- `memory/credentials-state.md` — superseded the "off-Mac LIVE / op_health verified" claim with the
  2026-06-16 DOWN state (host SA-token error; Mac route; FV#7 impact; host-side fix). This is the
  CANONICAL op-mcp status.
- `memory/MEMORY.md` — index line updated to the DOWN state.
- `surfaces/{claude-ai-web,iphone,README}.md` + `surfaces/always-loaded-instructions.md` — removed
  absolute "op-mcp LIVE" claims → point to `credentials-state.md`; switched "paste into Project
  custom-instructions" → "Settings > Profile > Preferences (global)" per the owner's choice.
- `PLAN.md` — 10.2 → [x]; Final-verification #7 annotated FAILING with the reason.
- Freshness gate green after all edits; residual stale sweep clean.

## Open / next (in priority order)
1. **10.3 — final verification.** Plugin-independent items already green (handoffs 023/025/026).
   Remaining: run `surface-check` on each of web / iPhone / Cowork (confirms 10.2 + answers
   "does Cowork run hooks" → record in surfaces/cowork.md + PLAN 7.2); #7 is currently FAILING
   (op-mcp, below); #8 non-Mac backup reaching OneDrive+Obsidian; #14 a live `/do`+`status` run.
2. **Fix the op-mcp host token (owner/Mac).** Check/rotate `OP_SERVICE_ACCOUNT_TOKEN` on the Render
   op-mcp service; confirm SA vault-read perms; re-verify `op_health`; then re-supersede
   credentials-state.md and clear the FV#7 annotation.
3. **Brain cleanup (pending owner confirm "delete all 6").** Remove the 6 test `.md` source files
   from the obsidian vault via the Mac `shell_exec` (the brain connector has NO delete tool) and
   clear the 2 actionable flags; let the index rebuild.

## Gotchas / context
- This session: hosted `op_*` tools are DOWN — resolve secrets via the Mac only.
- The op-mcp DOWN state may be transient (token rotation / Render redeploy). It's recorded as
  current fact with a "re-supersede once green" note; don't treat as permanent.
- Numbering: 025 Part 11 → 026 plugin-install → 027 (this).
