# Handoff 022 — QUEUED for Code: install claude-kit as a marketplace plugin (Part 10.1 deploy + hook de-dup)

Date: 2026-06-16. Authored on: claude.ai web. Status: manifests authored + pushed; INSTALL/VERIFY pending a Claude Code session on Schnapps-MBP.

## Done (authored from web, pushed to main)
- `.claude-plugin/marketplace.json` — marketplace `claude-kit`, one plugin `claude-kit-core`, `source: ./plugins/core`.
- `plugins/core/.claude-plugin/plugin.json` — minimal manifest; components auto-discover from the plugin root.
- `plugins/core/hooks/hooks.json` — **SessionEnd removed**; plugin now delivers ONLY SessionStart gate + Stop push-gate (decision 0005: backup is claude-kit-specific and stays project-scoped).
- Manifests have ZERO runtime effect until the plugin is installed. claude-kit `.claude/settings.json` still wires all three hooks (dogfood) until step 4 below.

## Why this must run on Code (not web)
Hooks only fire on Claude Code, and the de-dup is order-coupled: install first, confirm the plugin's hooks fire, THEN remove the duplicates from project settings — doing only one half leaves either a double-fire or a coverage gap. Workspace-trust must be accepted or hooks + the memory lane are silently nullified (decision 0005 / handoff 009).

## Ready-to-run prompt for the Code session
```
This is claude-kit (repo SchnappAPI/claude-kit, ~/code/claude-kit on Schnapps-MBP). Run the
session-hygiene skill at start. Execute Part 10.1 deployment per handoff 022.

The plugin manifests are authored + pushed. Marketplace "claude-kit", plugin "claude-kit-core"
(source ./plugins/core). The plugin's hooks.json delivers ONLY the SessionStart freshness/git gate
and the Stop push-gate (decision 0005); the SessionEnd backup stays project-scoped to claude-kit.

Steps:
1. git -C ~/code/claude-kit pull --ff-only
2. Install the plugin: `/plugin marketplace add ~/code/claude-kit` then
   `/plugin install claude-kit-core@claude-kit`. Accept the workspace-trust dialog if prompted
   (hooks + the memory lane are nullified until trust is accepted — decision 0005 / handoff 009).
3. Verify global delivery: confirm via `/hooks` that SessionStart `session-start-gate.sh` and Stop
   `session-stop-push-gate.sh` resolve under the plugin root. Open a fresh session in an UNRELATED
   repo and confirm the freshness gate fires there and the backup does NOT. (This is the PLAN 7.2 goal.)
4. De-dup (decision 0005): edit ~/code/claude-kit/.claude/settings.json — remove the SessionStart gate
   and Stop push-gate hook entries, keep ONLY the SessionEnd backup hook (leave autoMemoryDirectory and
   everything else). Restart Claude Code in claude-kit.
5. Verify each fires exactly once in claude-kit: SessionStart gate (plugin), Stop push-gate (plugin),
   SessionEnd backup (project) — no double-fire, no gap.
6. Run PLAN.md "Final verification" items 2, 3, 11 (no double hooks; global lane loads in every repo;
   a session with unmerged work addresses it first).
7. Mark PLAN 7.2 + 10.1 done. Append PROGRESS, write handoffs/024, commit + push. Run session-hygiene wrap.

Acceptance: gate + push-gate fire in a non-claude-kit repo; all three fire exactly once in claude-kit;
backup never fires outside claude-kit.
```

## After 10.1
- 10.2 — wire the other surfaces: connect the repo in Cowork; add core + domain skills and the op-mcp
  connector in claude.ai + iPhone; enable session-hygiene / surface-check per surface.
- 10.3 — run the full 14-point verification against the complete system.
- Part 11 — agentic-OS capstone (scheduler, `/do` orchestrator, `status` control plane).

## Pointers
- Schema source: working schnapp-kit manifests + plugin-structure skill. Split rationale: decision 0005.
- Carryover (today): one approved `service_restart` call to runtime-verify the new graceful path;
  decide whether `flask_restart` should also go graceful.
