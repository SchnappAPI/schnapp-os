# obsidian-mcp service secrets — resolved by op-wrap.sh at launchd startup.
# References only, never values (secrets-as-references rule).
MAC_MCP_AUTH_TOKEN="op://web-variables/MAC_MCP_AUTH_TOKEN/credential"
