# Runtime deps for server.py. PINNED DELIBERATELY: mcp's auth-provider API changed
# between minor versions and silently broke the entire OAuth flow (see decisions/0009).
# Bump only after re-running the end-to-end OAuth test. requirements.lock.txt has the
# exact resolved environment.
mcp==1.27.2
uvicorn==0.49.0
starlette==1.3.1
pydantic==2.13.4
