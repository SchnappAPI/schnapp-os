# mac-mcp service secrets — resolved by op-wrap.sh at launchd startup.
# GH_PAT here is scoped to the mac-mcp fine-grained PAT, distinct from the
# schnapp-bet web-plist PAT in the global .env.template.
GH_PAT="op://web-variables/GitHub/pat_web_plist"
MAC_MCP_AUTH_TOKEN="op://web-variables/MCP Tokens/schnapp_mac"
